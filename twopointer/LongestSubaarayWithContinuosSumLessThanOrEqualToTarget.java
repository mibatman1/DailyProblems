package dailyproblems.twopointer;

public class LongestSubaarayWithContinuosSumLessThanOrEqualToTarget {

    public int longestSubarray(int[] nums, int limit) {
        int l=0, r=0, len=Integer.MAX_VALUE;
        while(r<nums.length)
        {
            if(nums[])
        }
        return len;
    }
}
